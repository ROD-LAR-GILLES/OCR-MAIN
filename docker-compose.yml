services:
  ocr:
    build: .
    volumes:
      - ./pdfs:/app/pdfs
      - ./resultado:/app/resultado
    environment:
      - PYTHONPATH=/app/src
      - DISPLAY=:99
    stdin_open: true
    tty: true
    command: python -m interfaces.cli.interactive_menu